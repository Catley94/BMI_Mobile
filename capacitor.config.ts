import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.catley.bmi',
  appName: 'Check your BMI',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
